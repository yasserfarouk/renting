
REM Repository: external/RL-negotiation
echo Restoring: external/RL-negotiation
if exist "external\RL-negotiation\.git" (
    echo   Directory already exists, skipping...
) else (
    REM Create parent directory if needed
    if not exist "external" mkdir "external"
    
    REM Clone the repository
    git clone "git@github.com:yasserfarouk/renting.git" "external\RL-negotiation"
    if errorlevel 1 (
        echo   Failed to clone
    ) else (
        echo   Successfully cloned
        
        REM Checkout the original branch if not already on it
        cd "external\RL-negotiation"
        git checkout "main" 2>nul
        if errorlevel 1 (
            echo   Could not checkout branch: main
        ) else (
            echo   Checked out branch: main
        )
        cd ..\..
    )
)
echo.


REM Repository: external/RL-negotiation
echo Restoring: external/RL-negotiation
if exist "external\RL-negotiation\.git" (
    echo   Directory already exists, skipping...
) else (
    REM Create parent directory if needed
    if not exist "external" mkdir "external"
    
    REM Clone the repository
    git clone "git@github.com:yasserfarouk/renting.git" "external\RL-negotiation"
    if errorlevel 1 (
        echo   Failed to clone
    ) else (
        echo   Successfully cloned
        
        REM Checkout the original branch if not already on it
        cd "external\RL-negotiation"
        git checkout "main" 2>nul
        if errorlevel 1 (
            echo   Could not checkout branch: main
        ) else (
            echo   Checked out branch: main
        )
        cd ..\..
    )
)
echo.


REM Repository: external/RL-negotiation
echo Restoring: external/RL-negotiation
if exist "external\RL-negotiation\.git" (
    echo   Directory already exists, skipping...
) else (
    REM Create parent directory if needed
    if not exist "external" mkdir "external"
    
    REM Clone the repository
    git clone "git@github.com:yasserfarouk/renting.git" "external\RL-negotiation"
    if errorlevel 1 (
        echo   Failed to clone
    ) else (
        echo   Successfully cloned
        
        REM Checkout the original branch if not already on it
        cd "external\RL-negotiation"
        git checkout "main" 2>nul
        if errorlevel 1 (
            echo   Could not checkout branch: main
        ) else (
            echo   Checked out branch: main
        )
        cd ..\..
    )
)
echo.


REM Repository: external/RL-negotiation
echo Restoring: external/RL-negotiation
if exist "external\RL-negotiation\.git" (
    echo   Directory already exists, skipping...
) else (
    REM Create parent directory if needed
    if not exist "external" mkdir "external"
    
    REM Clone the repository
    git clone "git@github.com:yasserfarouk/renting.git" "external\RL-negotiation"
    if errorlevel 1 (
        echo   Failed to clone
    ) else (
        echo   Successfully cloned
        
        REM Checkout the original branch if not already on it
        cd "external\RL-negotiation"
        git checkout "main" 2>nul
        if errorlevel 1 (
            echo   Could not checkout branch: main
        ) else (
            echo   Checked out branch: main
        )
        cd ..\..
    )
)
echo.

